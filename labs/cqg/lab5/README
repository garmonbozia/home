В данной лабораторной реализован пример контейнера, работающий по принципу
FIFO, удовлетворяющий всем поставленным требованиям. Контейнер mQueue
(mqueue.h) наследован от стандартного контейнера queue приватным образом,
только лишь нужные пользователю методы (оговоренные в формулировке задания)
доступны снаружи. 
При добавлении нового элемента, равного уже существующему в очереди,
производится замена старого на новый (вариант 2).

Проверка работы данного контейнера производится с помощью тестового класса
wComplex (wcomplex.h), оператор `==` которого перегружен таким образом, чтобы
для неэквивалентных элементов данного класса выполнялось их "равенство".

К выполненной лабораторной приложен makefile с таргетами compile, run и all.

Лукиных Илья МП-40.

